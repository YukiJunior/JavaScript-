const nome = "Yuki"

array = []
array.push(1)
console.log(array)

let palavrasSimples = ["oi", "ok"]

console.log(nome)

for (let i = 0; i < palavrasSimples.length; i++) {
    const element = palavrasSimples[i];
    
}